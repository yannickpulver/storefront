# Store Helper — Project Instructions

## Changelog & Versioning

When shipping notable changes (features, fixes, refactors):
1. Update `CHANGELOG.md` — add entry under a new or existing version heading
2. Bump `version` in `package.json` following semver (major.minor.patch)
